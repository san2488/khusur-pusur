<table>
  <tr>
    <th>Voter</th>
    <th>Email</th>
  </tr>

  <% @votes.each do |vote| %>
      <tr>
        <td><%= vote.user.name %></td>
        <td><%= vote.user.email %></td>
      </tr>
  <% end %>
</table>